#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
#set($isEditable = true)

#set($content = $cms.content)

#if(!$content || $content.typeName != "White Paper")
	#foreach($item in $cms.filteredLinks("White Paper", 1, 1))
		#set($content = $item)
	#end
#end

#set($glcInputItem = $content)
#set($glcTagName = "relatedItems")
$cms.template("Get Linked Content")
#set($relatedItems = $glcOutputArray)

#set($trends = $util.newArrayList)
#foreach($catObj in $content.getCategorization().getCategories(7834))
	#if($catObj.name != "Uncategorized")
		#set($foo = $trends.add($catObj))
	#end
#end


$cms.template("Top")

<div class="headline">$cms.edit($content, "title")</div>

<div id="newsarticle">
	$cms.template("Networking Tools")
	
	<div class="calloutText">
		$util.date("MMMM dd, yyyy", $content.startDate)
	</div>
	
	<div class="bodyCopy">
		#if($relatedItems.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">RELATED ITEMS</div>
						<ul>
							<li>
								#if($req.param("cmsedit") == "Y")
									<span>$cms.edit($item, "title")</span>
								#else
									<a href="$item.url">$item.title</a> 
								#end
							</li>						
						</ul>
					</div>
				</div>
			</div>
		#end
	
		#if($content.images.size() > 0)
			<div class="contentRight" style="clear:right;">
				<div class="infoWrapperRight">
					<div class="infoUnder">
						<div class="infoHeader">PRODUCT IMAGES</div>

						<div class="relatedImages">
							#foreach($image in $content.images)
								#set($grioInputImage = $image)
								#set($grioMaxWidth = 220)
								$cms.template("Get Resized Image Object")
								$grio.imageTag<br/>
								#if($image.caption && $image.caption != "")
									$image.caption
									<br/>
								#end
								<br/>
							#end
						</div>
					</div>
				</div>
			</div>
		#end

		<div class="bodyCopy">$cms.edit($content, "body")</div>
		#if($content.whitePaperDoc) 
			#foreach($doc in $content.whitePaperDoc)
				#set($mediaObj = $cms.media($doc.id))
				<a href="$wma.getMediaTrackingUrl($mediaObj)" target="_new">$util.tag($cms.media("call_to_action_dwn.png","designimages"), ' alt="download $doc.title"')</a>
			#end
		#end
		
		#if($trends.size() > 0)
			<br/>
			<span class="tag-label">Trends: </span>

			#foreach($catObj in $trends)
				<span class="tags">$catObj.name</span>
			#end
			<br/><br/>
		#end
	</div>
</div>
		
$cms.template("Bottom")
